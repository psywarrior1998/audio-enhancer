# --- Caches and Logs ---
# These files are generated during runtime and are specific to a single session.
# They do not belong in the repository.
__pycache__/
*.py[cod]
*$py.class
*.log
.coverage
.cache
.pytest_cache/
.hypothesis/
.idea/

# --- Virtual Environments ---
# The virtual environment contains installed libraries, not your source code.
# The requirements.txt file defines the environment; the folder itself is disposable.
# Standard names are included for broad compatibility.
venv/
.venv/
env/
.env/
venv_audio_tool/ # From your original project
ENV/
env.bak/
venv.bak/

# --- Build, Packaging, and Distribution ---
# Artifacts generated by build and packaging tools like setuptools, wheel, and pyinstaller.
# Your source code is the authority; these are just compiled outputs.
build/
dist/
downloads/
eggs/
.eggs/
parts/
sdist/
develop-eggs/
*.egg-info/
*.egg
MANIFEST

# --- Large Data and Model Files ---
# NEVER commit large data files or trained models directly to Git.
# Use Git LFS (Large File Storage) or a dedicated data store (like DVC, S3) instead.
# These entries prevent accidental commits.
*.csv
*.json
*.parquet
*.h5
*.hdf5
*.pkl
*.pickle
*.joblib
*.dat
*.data
*.model
*.weights
*.pt  # PyTorch models
*.pth # PyTorch models
*.pb  # TensorFlow models

# --- Project-Specific Outputs ---
# Folders that your script is designed to create.
# The code that generates these files should be in the repo, not the files themselves.
output_audio/
input_audio/
models/
notebooks/html/ # Rendered HTML from notebooks

# --- IDE and Editor Configuration ---
# User-specific settings for code editors. Committing these forces your personal
# editor setup on all collaborators.
.idea/      # JetBrains IDEs (PyCharm, etc.)
.vscode/    # Visual Studio Code (unless sharing specific settings in a .vscode/settings.json)
*.sublime-project
*.sublime-workspace
.project
.pydevproject
.spyderproject
.ropeproject

# --- Operating System Files ---
# OS-specific metadata files that have no place in a shared code repository.
.DS_Store          # macOS
._* # macOS
.Spotlight-V100    # macOS
.Trashes           # macOS
Thumbs.db          # Windows
ehthumbs.db        # Windows
Desktop.ini        # Windows

# --- Security and Secrets ---
# Absolutely critical. NEVER commit secret keys, API tokens, or configuration files
# containing sensitive information. Use environment variables or a secrets management system.
*.env
.env
secrets.yaml
secrets.json
*.pem
*.key
credentials.json
config.py # Often contains secrets, be careful

# --- Jupyter Notebook ---
# The .ipynb_checkpoints directory is an autosave/recovery feature for Jupyter.
.ipynb_checkpoints/